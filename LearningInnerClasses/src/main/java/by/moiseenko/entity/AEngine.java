package by.moiseenko.entity;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class AEngine {
  static final Logger ALOG = LogManager.getLogger(AEngine.class.getName());
}
